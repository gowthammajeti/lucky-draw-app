# Application name
spring.application.name=backend-luckydraw

# Server port
server.port=8080

# MongoDB connection (default local setup)
spring.data.mongodb.uri=mongodb://localhost:27017/luckydrawdb

# Logging level
logging.level.org.springframework=INFO
logging.level.com.example.luckydraw=DEBUG
